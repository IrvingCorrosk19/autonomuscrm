@page
@model AutonomusCRM.API.Pages.AgentsModel
@{
    ViewData["Title"] = "Agentes IA - AUTONOMUS CRM";
}

<!-- TOPBAR -->
<header class="topbar">
  <div class="topbar-left">
    <div class="kicker">Agentes autónomos</div>
    <h2 class="headline">Sistema multi-agente en tiempo real</h2>
  </div>

  <div class="topbar-center">
    <div class="search" role="search">
      <input type="text" placeholder="Buscar agentes, decisiones, acciones..." aria-label="Buscar" />
      <span class="chip">Filtros</span>
      <span class="chip">Estado</span>
    </div>
  </div>

  <div class="topbar-right">
    <button class="btn mobile-toggle" type="button" onclick="toggleSidebar()">Menú</button>
    <button class="btn" type="button" onclick="showAllAgentsConfig()">Configurar</button>
    <a href="/Audit" class="btn primary crm-link-btn">Ver auditoría</a>
  </div>
</header>

<!-- STATS -->
<section class="stats" aria-label="Métricas de agentes">
  <div class="stat">
    <div class="label">Agentes activos</div>
    <div class="value">7 / 7</div>
    <div class="hint">
      <span>100% operativo</span>
      <span class="delta up">✓</span>
    </div>
  </div>

  <div class="stat">
    <div class="label">Decisiones hoy</div>
    <div class="value">1,247</div>
    <div class="hint">
      <span>Acciones ejecutadas</span>
      <span class="delta up">+18%</span>
    </div>
  </div>

  <div class="stat">
    <div class="label">Confianza promedio</div>
    <div class="value">0.84</div>
    <div class="hint">
      <span>Score de IA</span>
      <span class="delta up">+0.05</span>
    </div>
  </div>

  <div class="stat">
    <div class="label">Tiempo respuesta</div>
    <div class="value">2.3s</div>
    <div class="hint">
      <span>Promedio</span>
      <span class="delta up">-0.4s</span>
    </div>
  </div>
</section>

<section class="grid">
  <!-- LEFT: Agents List -->
  <div class="card">
    <div class="card-h">
      <div>
        <h3 class="title">Agentes autónomos</h3>
        <p class="subtitle">
          Estado, actividad y decisiones de cada agente especializado del sistema.
        </p>
      </div>
      <span class="pill">7 agentes</span>
    </div>

    <div class="card-b">
      <div class="feed">
        <div class="item">
          <div class="itop">
            <div class="itag">LEAD INTELLIGENCE</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("LeadIntelligenceAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("LeadIntelligenceAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Analiza y califica leads automáticamente. 342 leads procesados hoy, score promedio 72.
            <div class="crm-meta-muted">
              Confianza mínima: @(Model.AgentConfigs.GetValueOrDefault("LeadIntelligenceAgent")?.GetValueOrDefault("MinConfidence") ?? 0.7)
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 342</span>
            <span class="pill">Confianza: 0.87</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('LeadIntelligenceAgent')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">CUSTOMER RISK</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("CustomerRiskAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("CustomerRiskAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Monitorea riesgo de churn y calcula LTV. 23 clientes en riesgo detectados, 5 acciones recomendadas.
            <div class="crm-meta-muted">
              Umbral alto riesgo: @(Model.AgentConfigs.GetValueOrDefault("CustomerRiskAgent")?.GetValueOrDefault("HighRiskThreshold") ?? 70)
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 89</span>
            <span class="pill">Confianza: 0.82</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('CustomerRiskAgent')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">DEAL STRATEGY</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("DealStrategyAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("DealStrategyAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Optimiza estrategia de cierre y calcula probabilidades. 47 deals analizados, 12 recomendaciones.
            <div class="crm-meta-muted">
              Umbral LTV alto valor: @(Model.AgentConfigs.GetValueOrDefault("DealStrategyAgent")?.GetValueOrDefault("HighValueLTVThreshold") ?? 10000)
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 124</span>
            <span class="pill">Confianza: 0.85</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('DealStrategyAgent')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">COMMUNICATION</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("CommunicationAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("CommunicationAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Gestiona comunicaciones multicanal. 156 mensajes enviados, 89% tasa de apertura.
            <div class="crm-meta-muted">
              Email bienvenida: @((Model.AgentConfigs.GetValueOrDefault("CommunicationAgent")?.GetValueOrDefault("AutoWelcomeEmail")?.ToString() == "True") ? "Activado" : "Desactivado")
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 156</span>
            <span class="pill">Confianza: 0.79</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('CommunicationAgent')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">DATA QUALITY</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("DataQualityGuardian")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("DataQualityGuardian")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Detecta y corrige problemas de calidad de datos. 67 correcciones aplicadas, 12 pendientes.
            <div class="crm-meta-muted">
              Auto-corrección: @((Model.AgentConfigs.GetValueOrDefault("DataQualityGuardian")?.GetValueOrDefault("AutoFixEnabled")?.ToString() == "True") ? "Activada" : "Desactivada")
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 67</span>
            <span class="pill">Confianza: 0.91</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('DataQualityGuardian')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">COMPLIANCE & SECURITY</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("ComplianceSecurityAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("ComplianceSecurityAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Monitorea cumplimiento y seguridad. 0 incidentes, 234 verificaciones realizadas.
            <div class="crm-meta-muted">
              Bloqueo kill-switch: @((Model.AgentConfigs.GetValueOrDefault("ComplianceSecurityAgent")?.GetValueOrDefault("BlockOnKillSwitch")?.ToString() == "True") ? "Activado" : "Desactivado")
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 234</span>
            <span class="pill">Confianza: 0.95</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('ComplianceSecurityAgent')">Configurar</button>
          </div>
        </div>

        <div class="item">
          <div class="itop">
            <div class="itag">AUTOMATION OPTIMIZER</div>
            <div class="itime">
              <span class="badge @((Model.AgentConfigs.GetValueOrDefault("AutomationOptimizerAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "ok" : "danger")">
                @((Model.AgentConfigs.GetValueOrDefault("AutomationOptimizerAgent")?.GetValueOrDefault("IsEnabled")?.ToString() == "True") ? "Activo" : "Inactivo")
              </span>
            </div>
          </div>
          <div class="ibody">
            Optimiza workflows y procesos automáticos. 12 optimizaciones sugeridas, 8 aplicadas.
            <div class="crm-meta-muted">
              Aplicación automática: @((Model.AgentConfigs.GetValueOrDefault("AutomationOptimizerAgent")?.GetValueOrDefault("AutoApplyOptimizations")?.ToString() == "True") ? "Activada" : "Desactivada")
            </div>
          </div>
          <div class="ifooter">
            <span class="pill">Decisiones: 45</span>
            <span class="pill">Confianza: 0.88</span>
            <button class="btn crm-btn-mini" type="button" onclick="showAgentConfigModal('AutomationOptimizerAgent')">Configurar</button>
          </div>
        </div>
      </div>

      <div class="crm-toolbar-spacer"></div>

      <div class="crm-toolbar">
        <a href="/Audit" class="btn crm-link-btn">Ver auditoría</a>
        <button class="btn primary" type="button" onclick="showAllAgentsConfig()">Configurar todos</button>
      </div>
    </div>
  </div>

  <!-- RIGHT: Activity Feed -->
  <aside class="stack">
    <div class="card">
      <div class="card-h">
        <div>
          <h3 class="title">Actividad reciente</h3>
          <p class="subtitle">Últimas decisiones y acciones de los agentes autónomos.</p>
        </div>
        <span class="pill">Tiempo real</span>
      </div>
      <div class="card-b">
        <div class="feed">
          <div class="item">
            <div class="itop">
              <div class="itag">DECISIÓN</div>
              <div class="itime">Hace 2 min</div>
            </div>
            <div class="ibody">
              Lead Intelligence: Lead "Juan Pérez" calificado con score 85. Recomendación: enviar propuesta.
            </div>
            <div class="ifooter">
              <span class="pill">Confianza: 0.87</span>
              <span class="pill">Aprobada</span>
            </div>
          </div>

          <div class="item">
            <div class="itop">
              <div class="itag">ALERTA</div>
              <div class="itime">Hace 5 min</div>
            </div>
            <div class="ibody">
              Customer Risk: Cliente "Comercio DEF" en riesgo alto (score 78). Acción: retención urgente.
            </div>
            <div class="ifooter">
              <span class="pill">Confianza: 0.82</span>
              <span class="pill">Pendiente</span>
            </div>
          </div>

          <div class="item">
            <div class="itop">
              <div class="itag">OPTIMIZACIÓN</div>
              <div class="itime">Hace 8 min</div>
            </div>
            <div class="ibody">
              Automation Optimizer: Workflow "Lead Follow-up" optimizado. Tiempo reducido 23%.
            </div>
            <div class="ifooter">
              <span class="pill">Confianza: 0.88</span>
              <span class="pill">Aplicada</span>
            </div>
          </div>
        </div>

        <div class="crm-toolbar-spacer"></div>

        <a href="/Audit" class="btn primary crm-btn-block crm-link-btn">Ver auditoría completa</a>
      </div>
    </div>

    <div class="card">
      <div class="card-h">
        <div>
          <h3 class="title">Rendimiento</h3>
          <p class="subtitle">Métricas de eficiencia y precisión de los agentes.</p>
        </div>
        <span class="pill">Últimas 24h</span>
      </div>
      <div class="card-b">
        <table class="table" aria-label="Rendimiento de agentes">
          <thead>
            <tr>
              <th>Agente</th>
              <th>Decisiones</th>
              <th>Precisión</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Lead Intelligence</td>
              <td>342</td>
              <td><span class="badge ok">87%</span></td>
            </tr>
            <tr>
              <td>Customer Risk</td>
              <td>89</td>
              <td><span class="badge ok">82%</span></td>
            </tr>
            <tr>
              <td>Deal Strategy</td>
              <td>124</td>
              <td><span class="badge ok">85%</span></td>
            </tr>
            <tr>
              <td>Communication</td>
              <td>156</td>
              <td><span class="badge warn">79%</span></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </aside>
</section>

<div class="crm-section-gap"></div>

<footer class="crm-footer-note">
  AUTONOMUS CRM · Sistema multi-agente autónomo con inteligencia distribuida.
</footer>


@if (TempData["ErrorMessage"] != null)
{
  <div class="crm-alert-floating error">
    <strong>✗ Error:</strong> @TempData["ErrorMessage"]
  </div>
}

<!-- Modal de configuración de agente -->
<div id="agentConfigModal" class="crm-overlay-modal crm-overlay-modal-scroll">
    <div class="card crm-overlay-card crm-overlay-card-xl crm-overlay-card-top">
        <div class="card-h">
            <div>
                <h3 class="title" id="agentConfigTitle">Configurar Agente</h3>
                <p class="subtitle" id="agentConfigSubtitle"></p>
            </div>
        </div>
        <div class="card-b">
            <div id="agentTutorial" class="crm-agent-tutorial">
                <div class="crm-agent-tutorial-icon">i</div>
                <div>
                    <h4 class="crm-agent-tutorial-title">Guía de configuración</h4>
                    <ul id="tutorialContent" class="crm-agent-tutorial-list"></ul>
                </div>
            </div>

            <form id="agentConfigForm" method="post" asp-page-handler="UpdateAgentConfig">
                <input type="hidden" id="agentNameInput" name="agentName" />
                <input type="hidden" id="configJsonInput" name="configJson" />
                <div class="crm-agent-config-grid">
                    <section class="crm-agent-config-section">
                        <h4 class="crm-agent-section-title">Configuración general</h4>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row">
                                <input type="checkbox" id="IsEnabled" />
                                <span>Agente activo</span>
                            </label>
                            <div class="crm-form-help">Activa o desactiva el procesamiento automático para este agente.</div>
                        </div>
                        <div class="crm-form-field">
                            <label for="MinConfidence">Confianza mínima (0.0 - 1.0)</label>
                            <input class="crm-form-control" type="number" id="MinConfidence" step="0.01" min="0" max="1" />
                            <div class="crm-form-help">Valores más altos reducen decisiones automáticas de baja certeza.</div>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="LeadIntelligenceAgent">
                        <h4 class="crm-agent-section-title">Pesos de scoring por fuente</h4>
                        <div class="crm-agent-fields-2">
                            <div class="crm-form-field">
                                <label for="SourceWeight_Referral">Referral</label>
                                <input class="crm-form-control" type="number" id="SourceWeight_Referral" />
                            </div>
                            <div class="crm-form-field">
                                <label for="SourceWeight_Website">Website</label>
                                <input class="crm-form-control" type="number" id="SourceWeight_Website" />
                            </div>
                            <div class="crm-form-field">
                                <label for="SourceWeight_SocialMedia">Social Media</label>
                                <input class="crm-form-control" type="number" id="SourceWeight_SocialMedia" />
                            </div>
                            <div class="crm-form-field">
                                <label for="SourceWeight_EmailCampaign">Email Campaign</label>
                                <input class="crm-form-control" type="number" id="SourceWeight_EmailCampaign" />
                            </div>
                        </div>
                        <h4 class="crm-agent-section-title">Pesos de contacto</h4>
                        <div class="crm-agent-fields-3">
                            <div class="crm-form-field">
                                <label for="ContactWeight_Email">Email</label>
                                <input class="crm-form-control" type="number" id="ContactWeight_Email" />
                            </div>
                            <div class="crm-form-field">
                                <label for="ContactWeight_Phone">Teléfono</label>
                                <input class="crm-form-control" type="number" id="ContactWeight_Phone" />
                            </div>
                            <div class="crm-form-field">
                                <label for="ContactWeight_Company">Empresa</label>
                                <input class="crm-form-control" type="number" id="ContactWeight_Company" />
                            </div>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="CustomerRiskAgent">
                        <h4 class="crm-agent-section-title">Riesgo de clientes</h4>
                        <div class="crm-agent-fields-2">
                            <div class="crm-form-field">
                                <label for="BaseRiskScore">Score base</label>
                                <input class="crm-form-control" type="number" id="BaseRiskScore" min="0" max="100" />
                            </div>
                            <div class="crm-form-field">
                                <label for="HighRiskThreshold">Umbral alto riesgo</label>
                                <input class="crm-form-control" type="number" id="HighRiskThreshold" min="0" max="100" />
                            </div>
                            <div class="crm-form-field">
                                <label for="LowRiskThreshold">Umbral bajo riesgo</label>
                                <input class="crm-form-control" type="number" id="LowRiskThreshold" min="0" max="100" />
                            </div>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="DealStrategyAgent">
                        <h4 class="crm-agent-section-title">Estrategia de deals</h4>
                        <div class="crm-agent-fields-2">
                            <div class="crm-form-field">
                                <label for="HighValueLTVThreshold">Umbral LTV alto valor</label>
                                <input class="crm-form-control" type="number" id="HighValueLTVThreshold" min="0" />
                            </div>
                            <div class="crm-form-field">
                                <label for="StagnantDaysThreshold">Días estancado</label>
                                <input class="crm-form-control" type="number" id="StagnantDaysThreshold" min="0" />
                            </div>
                            <div class="crm-form-field">
                                <label for="RiskProbabilityThreshold">Probabilidad de riesgo (%)</label>
                                <input class="crm-form-control" type="number" id="RiskProbabilityThreshold" min="0" max="100" />
                            </div>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="CommunicationAgent">
                        <h4 class="crm-agent-section-title">Comunicación automática</h4>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="AutoWelcomeEmail" /><span>Email de bienvenida automático</span></label>
                        </div>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="AutoFollowUp" /><span>Seguimiento automático</span></label>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="DataQualityGuardian">
                        <h4 class="crm-agent-section-title">Calidad de datos</h4>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="AutoFixEnabled" /><span>Auto-corrección habilitada</span></label>
                        </div>
                        <div class="crm-form-field">
                            <label for="ScanIntervalMinutes">Intervalo de escaneo (minutos)</label>
                            <input class="crm-form-control" type="number" id="ScanIntervalMinutes" min="1" />
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="ComplianceSecurityAgent">
                        <h4 class="crm-agent-section-title">Compliance y seguridad</h4>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="RequireCorrelationId" /><span>Requerir CorrelationId</span></label>
                        </div>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="RequireTenantId" /><span>Requerir TenantId</span></label>
                        </div>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="BlockOnKillSwitch" /><span>Bloquear con kill-switch activo</span></label>
                        </div>
                    </section>

                    <section class="crm-agent-config-section" data-agent-section="AutomationOptimizerAgent">
                        <h4 class="crm-agent-section-title">Optimización automática</h4>
                        <div class="crm-form-field">
                            <label class="crm-checkbox-row"><input type="checkbox" id="AutoApplyOptimizations" /><span>Aplicar optimizaciones automáticamente</span></label>
                        </div>
                        <div class="crm-agent-fields-2">
                            <div class="crm-form-field">
                                <label for="AnalysisIntervalMinutes">Intervalo de análisis (minutos)</label>
                                <input class="crm-form-control" type="number" id="AnalysisIntervalMinutes" min="1" />
                            </div>
                            <div class="crm-form-field">
                                <label for="OptimizationThreshold">Umbral de optimización (0.0 - 1.0)</label>
                                <input class="crm-form-control" type="number" id="OptimizationThreshold" step="0.01" min="0" max="1" />
                            </div>
                        </div>
                    </section>
                </div>
                <div class="crm-split-actions">
                    <button type="submit" class="btn primary">Guardar configuración</button>
                    <button type="button" class="btn" onclick="closeAgentConfigModal()">Cancelar</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
const agentConfigs = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.AgentConfigs));

const AGENT_METADATA = {
    LeadIntelligenceAgent: {
        displayName: 'Lead Intelligence Agent',
        description: 'Analiza y califica leads automáticamente basándose en fuente, datos y comportamiento.',
        tutorial: ['Activa el agente para procesar eventos de leads.', 'Ajusta confianza mínima según riesgo operativo.', 'Configura pesos de fuente y contacto para priorización comercial.']
    },
    CustomerRiskAgent: {
        displayName: 'Customer Risk Agent',
        description: 'Evalúa riesgo de churn y prioriza acciones de retención.',
        tutorial: ['Define score base y umbrales alto/bajo.', 'Mantén umbral alto conservador para alertas críticas.', 'Usa el umbral bajo para campañas de fidelización.']
    },
    DealStrategyAgent: {
        displayName: 'Deal Strategy Agent',
        description: 'Optimiza estrategia de cierre y detecta deals estancados.',
        tutorial: ['Configura LTV de alto valor.', 'Ajusta días estancado para alertas tempranas.', 'Define probabilidad mínima de riesgo para intervención.']
    },
    CommunicationAgent: {
        displayName: 'Communication Agent',
        description: 'Gestiona comunicaciones automáticas multicanal.',
        tutorial: ['Activa bienvenida automática para onboarding.', 'Activa seguimiento para continuidad comercial.', 'Valida impacto antes de automatizar al 100%.']
    },
    DataQualityGuardian: {
        displayName: 'Data Quality Guardian',
        description: 'Detecta y corrige problemas de calidad de datos.',
        tutorial: ['Activa auto-fix si el equipo lo aprobó.', 'Define intervalo de escaneo según volumen.', 'Mantén foco en consistencia y trazabilidad.']
    },
    ComplianceSecurityAgent: {
        displayName: 'Compliance & Security Agent',
        description: 'Monitorea cumplimiento, seguridad y trazabilidad.',
        tutorial: ['Mantén CorrelationId y TenantId obligatorios.', 'Activa bloqueo con kill-switch.', 'Prioriza seguridad sobre automatización.']
    },
    AutomationOptimizerAgent: {
        displayName: 'Automation Optimizer Agent',
        description: 'Optimiza workflows automáticamente según desempeño.',
        tutorial: ['Empieza con sugerencias manuales.', 'Sube el umbral para reducir cambios de bajo impacto.', 'Luego habilita auto-apply de forma gradual.']
    }
};

function showAgentConfigModal(agentName) {
    document.getElementById('agentNameInput').value = agentName;
    const metadata = AGENT_METADATA[agentName] || { displayName: agentName, description: '', tutorial: ['Revisa la configuración y aplica cambios de forma segura.'] };
    document.getElementById('agentConfigTitle').textContent = 'Configurar: ' + metadata.displayName;
    document.getElementById('agentConfigSubtitle').textContent = metadata.description;

    const tutorialEl = document.getElementById('tutorialContent');
    tutorialEl.innerHTML = '';
    metadata.tutorial.forEach((step) => {
        const li = document.createElement('li');
        li.textContent = step;
        tutorialEl.appendChild(li);
    });

    showAgentSection(agentName);
    bindConfigToForm(agentName, agentConfigs[agentName] || {});
    document.getElementById('agentConfigModal').style.display = 'flex';
}

function showAllAgentsConfig() {
    showAgentConfigModal('LeadIntelligenceAgent');
}

function closeAgentConfigModal() {
    document.getElementById('agentConfigModal').style.display = 'none';
}

function showAgentSection(agentName) {
    document.querySelectorAll('[data-agent-section]').forEach((section) => {
        section.style.display = section.getAttribute('data-agent-section') === agentName ? '' : 'none';
    });
}

function bindConfigToForm(agentName, config) {
    document.getElementById('IsEnabled').checked = config.IsEnabled === true;
    document.getElementById('MinConfidence').value = config.MinConfidence ?? 0.75;

    if (agentName === 'LeadIntelligenceAgent') {
        const sourceWeights = config.SourceWeights || {};
        const contactWeights = config.ContactWeights || {};
        document.getElementById('SourceWeight_Referral').value = sourceWeights.Referral ?? 30;
        document.getElementById('SourceWeight_Website').value = sourceWeights.Website ?? 20;
        document.getElementById('SourceWeight_SocialMedia').value = sourceWeights.SocialMedia ?? 15;
        document.getElementById('SourceWeight_EmailCampaign').value = sourceWeights.EmailCampaign ?? 10;
        document.getElementById('ContactWeight_Email').value = contactWeights.Email ?? 15;
        document.getElementById('ContactWeight_Phone').value = contactWeights.Phone ?? 10;
        document.getElementById('ContactWeight_Company').value = contactWeights.Company ?? 20;
    } else if (agentName === 'CustomerRiskAgent') {
        document.getElementById('BaseRiskScore').value = config.BaseRiskScore ?? 50;
        document.getElementById('HighRiskThreshold').value = config.HighRiskThreshold ?? 70;
        document.getElementById('LowRiskThreshold').value = config.LowRiskThreshold ?? 30;
    } else if (agentName === 'DealStrategyAgent') {
        document.getElementById('HighValueLTVThreshold').value = config.HighValueLTVThreshold ?? 10000;
        document.getElementById('StagnantDaysThreshold').value = config.StagnantDaysThreshold ?? 30;
        document.getElementById('RiskProbabilityThreshold').value = config.RiskProbabilityThreshold ?? 30;
    } else if (agentName === 'CommunicationAgent') {
        document.getElementById('AutoWelcomeEmail').checked = config.AutoWelcomeEmail === true;
        document.getElementById('AutoFollowUp').checked = config.AutoFollowUp === true;
    } else if (agentName === 'DataQualityGuardian') {
        document.getElementById('AutoFixEnabled').checked = config.AutoFixEnabled === true;
        document.getElementById('ScanIntervalMinutes').value = config.ScanIntervalMinutes ?? 60;
    } else if (agentName === 'ComplianceSecurityAgent') {
        document.getElementById('RequireCorrelationId').checked = config.RequireCorrelationId === true;
        document.getElementById('RequireTenantId').checked = config.RequireTenantId === true;
        document.getElementById('BlockOnKillSwitch').checked = config.BlockOnKillSwitch === true;
    } else if (agentName === 'AutomationOptimizerAgent') {
        document.getElementById('AutoApplyOptimizations').checked = config.AutoApplyOptimizations === true;
        document.getElementById('AnalysisIntervalMinutes').value = config.AnalysisIntervalMinutes ?? 120;
        document.getElementById('OptimizationThreshold').value = config.OptimizationThreshold ?? 0.1;
    }
}

document.getElementById('agentConfigForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const agentName = document.getElementById('agentNameInput').value;
    const config = {};
    
    config.IsEnabled = document.getElementById('IsEnabled').checked;
    config.MinConfidence = parseFloat(document.getElementById('MinConfidence').value);
    
    if (agentName === 'LeadIntelligenceAgent') {
        config.SourceWeights = {
            Referral: parseInt(document.getElementById('SourceWeight_Referral').value),
            Website: parseInt(document.getElementById('SourceWeight_Website').value),
            SocialMedia: parseInt(document.getElementById('SourceWeight_SocialMedia').value),
            EmailCampaign: parseInt(document.getElementById('SourceWeight_EmailCampaign').value)
        };
        config.ContactWeights = {
            Email: parseInt(document.getElementById('ContactWeight_Email').value),
            Phone: parseInt(document.getElementById('ContactWeight_Phone').value),
            Company: parseInt(document.getElementById('ContactWeight_Company').value)
        };
    } else if (agentName === 'CustomerRiskAgent') {
        config.BaseRiskScore = parseInt(document.getElementById('BaseRiskScore').value);
        config.HighRiskThreshold = parseInt(document.getElementById('HighRiskThreshold').value);
        config.LowRiskThreshold = parseInt(document.getElementById('LowRiskThreshold').value);
    } else if (agentName === 'DealStrategyAgent') {
        config.HighValueLTVThreshold = parseInt(document.getElementById('HighValueLTVThreshold').value);
        config.StagnantDaysThreshold = parseInt(document.getElementById('StagnantDaysThreshold').value);
        config.RiskProbabilityThreshold = parseInt(document.getElementById('RiskProbabilityThreshold').value);
    } else if (agentName === 'CommunicationAgent') {
        config.AutoWelcomeEmail = document.getElementById('AutoWelcomeEmail').checked;
        config.AutoFollowUp = document.getElementById('AutoFollowUp').checked;
    } else if (agentName === 'DataQualityGuardian') {
        config.AutoFixEnabled = document.getElementById('AutoFixEnabled').checked;
        config.ScanIntervalMinutes = parseInt(document.getElementById('ScanIntervalMinutes').value);
    } else if (agentName === 'ComplianceSecurityAgent') {
        config.RequireCorrelationId = document.getElementById('RequireCorrelationId').checked;
        config.RequireTenantId = document.getElementById('RequireTenantId').checked;
        config.BlockOnKillSwitch = document.getElementById('BlockOnKillSwitch').checked;
    } else if (agentName === 'AutomationOptimizerAgent') {
        config.AutoApplyOptimizations = document.getElementById('AutoApplyOptimizations').checked;
        config.AnalysisIntervalMinutes = parseInt(document.getElementById('AnalysisIntervalMinutes').value);
        config.OptimizationThreshold = parseFloat(document.getElementById('OptimizationThreshold').value);
    }
    
    document.getElementById('configJsonInput').value = JSON.stringify(config);
    this.submit();
});
</script>

