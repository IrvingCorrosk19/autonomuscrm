# TABLE_SYSTEM_ENTERPRISE

## Estándar definido

- Sticky header en desktop.
- Wrapper responsive automático (`site.js`).
- Modo móvil de filas con `data-label`.
- Hover premium y densidad consistente.
- Compatibilidad con tablas legacy sin refactor invasivo.

## Estado
Aplicado globalmente a tablas `.table` vía JS + CSS.
