# ADVANCED_TABLE_SYSTEM_FINAL

- Navegación por teclado en filas (`ArrowUp`/`ArrowDown`) incorporada en tablas operacionales.
- Densidad visual preparada con `crm-density-toggle`.
- Estados vacíos y CTA de tabla unificados con clases `crm-*`.
- Base lista para evolución a bulk actions contextuales sin romper estabilidad actual.
