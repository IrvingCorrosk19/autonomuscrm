# WORKFLOW_UI_REFINEMENT

- `Workflows.cshtml` migrado de inline a `crm-*` para acciones, vacios, toolbar, modales y footer.
- CTA principal y secundarios ahora usan patrones de bloque/compacto consistentes.
- Modales de importacion alineados al mismo layout que `Users` y `Policies`.
- Resultado: menor sensacion "CRUD legacy", mayor coherencia operacional enterprise.
