# ACCESSIBILITY_QA_FINAL

- Flujo keyboard-only reforzado en tablas y modales overlay.
- Focus trap base implementado para `crm-overlay-modal`.
- Cierre accesible por `Esc` y click fuera del modal.
- `aria-live` en toasts preservado para feedback no bloqueante.
